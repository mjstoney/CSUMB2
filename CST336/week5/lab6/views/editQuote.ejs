<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CRUD Operations</title>
  <!-- CSS only -->
  <link rel="stylesheet" href="/css/style.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">


</head>
<body>
  <h1>Updating a new Quote</h1>
    <form method="POST" action="/quote/edit">
        <input type="hidden" name="quoteId" value="<%=quoteInfo[0].quoteId%>">
      First Name:
      <input type="text" name="fName" value="<%= quoteInfo[0].firstName %>"><br>
      Last Name:
        <input type="text" name="lName" value="<%= quoteInfo[0].lastName %>"><br>
        Categories: 
        <select name="category">
            <option disabled value="">Categories</option>
            <% for (category of categories) { %>}
            <option value="<%=category.category %>"><%=category.category %></option>
            <% } %>
        </select><br><br>
        Quote:
        <textarea name="quote" cols="30" rows="10"> <%= quoteInfo[0].quote %></textarea><br><br>

      <button class="btn btn-primary mt-5">Update Quote</button>
    </form>
    <% if (typeof message != "undefined"){ %>
        <h3><%=message%></h3><br>
        <a href="/quotes">Quotes</a>
        <a href="/">Home</a>
<% } %>

</body>
</html>
